{
  "name": "inbox-wants-dashboard",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Private Cloudflare Pages dashboard for Supabase idea inbox, wants, and next actions.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "dev:pages": "npm run build && wrangler pages dev dist --port 4183",
    "typecheck": "tsc --noEmit",
    "test": "node --test --test-isolation=none tests/*.test.ts",
    "check": "node --check public/app.js && npm run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "wrangler": "^4.37.1"
  }
}
