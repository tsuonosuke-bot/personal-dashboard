{
  "name": "personal-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal Hub for Compass, finance, and knowledge dashboards.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "dev:pages": "npm run build && wrangler pages dev dist --port 4183",
    "typecheck": "tsc --noEmit",
    "test": "node --test --test-isolation=none tests/*.test.ts",
    "check": "node --check public/api-client.js && node --check public/app.js && node --check public/hub.js && node --check public/habits.js && node --check public/projects.js && node --check public/status/status.js && node --check public/writing.js && npm run typecheck",
    "predeploy:check": "npm test && npm run check && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "wrangler": "^4.37.1"
  },
  "dependencies": {
    "jose": "^6.2.12"
  }
}
